# 저자지시 (AUTHOR DIRECTIVES)

> **이 파일은 저자(사용자)만 수정한다. 에이전트는 읽기 전용 — 편집·정리·재구성·형식 통일 등 어떤 변경도 금지.**
> 지시가 충돌하면 우선순위: **이 파일 > CLAUDE.md > 실험일지·README > 대화 맥락.**
> 에이전트는 세션 시작 시와 방향이 걸린 결정 전에 이 파일을 먼저 확인한다.

---

## 1. 연구 방향 (현재 유효)

<!-- 아래는 에이전트가 초기값으로 옮겨 적은 기존 결정 — 수정·삭제 전부 저자 자유 -->

- 강건성 사다리 6단 순서대로: K20 → A12 → K50 → CFR+구현 → CFR학습(K20) → CFR학습(K50). 한 번에 한 변수만.
- 데이터 먼저 쌓고, 투고 방식(강건성 절 vs 전면 재기준선화)은 나중에 판단한다.
- 과거 코드·폴더는 동결, 신규 실험은 poker-ladder/에서 새 코드로만 진행.

## 2. 하지 말 것 (금지)

- 이 파일 수정 금지.
- 레거시 코드(poker-pokerkit*, 구 results) 리팩토링·재배치 금지.
- 한 비교 안에서 두 변수 동시 변경 금지.

## 3. 보류/유보 (건드리지 말 것)

- 팟/SPR 상태 추가 = 7단 후보. 게이트(천장 불변 + E10 로그 진단) 전에 착수 금지.
- 투고 마감 3종(통계검정 명기·그림 배치·저자정보)은 저자 판단 대기.

## 4. 에이전트 작업 방식에 대한 의견

- 응답은 자연어 설명 1번 + 데이터 + 끝. "왜"는 내가 물으면 답한다.
- 방향을 통째로 위임하지 않는다. 한 번에 목표 하나, 게이트는 저자가.

## 5. 메모 (자유)

- 백그라운드로 돌리지 말고 진행상황 백분율로 표기해서 사용자가 실시간으로 확인할 수 있게 할것
터미널이든 gui든 cli든 방식 상관없이 
